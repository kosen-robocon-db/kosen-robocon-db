<%= label_tag "", "完了課題：" %>
<% concat d.progress.to_bool ? "記録有り" : "記録無し" %>
<% if d.progress.to_bool %>
  <div class="indent">
    <%= label_tag "", "#{@game.left_robot.name}：", class: 'robot_name' %>
    <div class="indent">
      <%# この指定方法は値と順番が異なると意図しない文言が表示されてしまうので
          改善されるべき %>
      <%= "#{klass.progresses_i18n.to_a[d.my_progress.to_i][1]}" %>
    </div>
    <%#----------------------------------------------------------------------%>
    <%= label_tag "", "#{@game.right_robot.name}：", class: 'robot_name' %>
    <div class="indent">
      <%# この指定方法は値と順番が異なると意図しない文言が表示されてしまうので
          改善されるべき %>
      <%= "#{klass.progresses_i18n.to_a[d.opponent_progress.to_i][1]}" %>
    </div>
  </div>
<% end %>
