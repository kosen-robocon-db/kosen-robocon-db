<%= f.hidden_field :id %>
<%
  ball_touch_numbers = option_numbers digit: 1, end_number: 9
  intercept_numbers  = option_numbers digit: 1, end_number: 3
  retry_numbers      = option_numbers digit: 1, end_number: 9
  vote_numbers       = option_numbers digit: 1, end_number: 3
%>
<table>
  <tr>
    <td><%= f.label :my_time, "#{@robot.name}のタッチダウンパス成功タイム：" %></td>
    <td>
      <%= f.select :my_time_minute, time_numbers %>分
      <%= f.select :my_time_second, time_numbers %>秒
    </td>
  </tr>
  <tr>
    <td><%= f.label :opponent_time, "対戦相手のタッチダウンパス成功タイム：" %></td>
    <td>
      <%= f.select :opponent_time_minute, time_numbers %>分
      <%= f.select :opponent_time_second, time_numbers %>秒
    </td>
  </tr>
</table>
<table>
  <tr>
    <td><%= f.label :my_ball_touch, "#{@robot.name}のボールタッチ：" %></td>
    <td><%= f.select :my_ball_touch, ball_touch_numbers %>回</td>
    </td>
  </tr>
  <tr>
    <td><%= f.label :opponent_ball_touch, "対戦相手のボールタッチ：" %></td>
    <td><%= f.select :opponent_ball_touch, ball_touch_numbers %>回</td>
    </td>
  </tr>
</table>
<table>
  <tr>
    <td><%= f.label :my_intercept, "#{@robot.name}のインターセプト：" %></td>
    <td><%= f.select :my_intercept, intercept_numbers %>回</td>
    </td>
  </tr>
  <tr>
    <td><%= f.label :opponent_intercept, "対戦相手のインターセプト：" %></td>
    <td><%= f.select :opponent_intercept, intercept_numbers %>回</td>
    </td>
  </tr>
</table>
<table>
  <tr>
    <td><%= f.label :my_retry, "#{@robot.name}のリトライ：" %></td>
    <td><%= f.select :my_retry, retry_numbers %>回</td>
    </td>
  </tr>
  <tr>
    <td><%= f.label :opponent_retry, "対戦相手のリトライ：" %></td>
    <td><%= f.select :opponent_retry, retry_numbers %>回</td>
    </td>
  </tr>
</table>
<%= f.label :jury_votes, "審査員判定：" %>
<%= f.check_box :jury_votes, {:checked => f.object.jury_votes}, true, false %>
<br />
<% if f.object.jury_votes %>
<div class="jury_votes___nested_field_for_replace_with_index__">
<% else %>
<div class="jury_votes___nested_field_for_replace_with_index__ hidden">
<% end %>
  <table>
    <tr>
      <td><%= ("&nbsp;"*4).html_safe %></td>
      <td><%= f.label :my_jury_votes, "#{@robot.name}への票数：" %></td>
      <td><%= f.select :my_jury_votes, vote_numbers %></td>
    </tr>
    <tr>
      <td><%= ("&nbsp;"*4).html_safe %></td>
      <td><%= f.label :opponent_jury_votes, "対戦相手への票数：" %></td>
      <td><%= f.select :opponent_jury_votes, vote_numbers %></td>
    </tr>
  </table>
</div>
<%= f.label :memo, "メモ：" %>
<%= f.text_field :memo %><br />
<br />
<%= f.hidden_field :_destroy %>
<%= f.remove_nested_fields_link '削除', class: 'btn btn-danger',
  role: 'button' %><br />
