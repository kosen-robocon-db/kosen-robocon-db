<% provide(:title, "優勝／受賞歴の編集") %>

<%= render "shared/title", title: (yield :title) %>

<h4><%= @prize_history.robot.name %><br></h4>

<%= render "form", prize_history: @prize_history,
  action: :update, button_label: "更新" %>
